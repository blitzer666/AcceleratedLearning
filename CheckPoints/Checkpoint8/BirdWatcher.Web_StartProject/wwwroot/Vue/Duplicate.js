const app = new Vue({
    el: "#app",
    data: {
        firstname: ""
    },
    computed: {
        function () {
            return 🐼;
        },
        //loud: function () {
        //    if (this.firstname.length > 0)
        //        return "**" + this.firstname.toUpperCase() + "**";
        //    else
                return "";
        }
    }
})